# Speed up installs by using cached packages when available
prefer-offline=true

# Suppress noise in CI and local installs
fund=false

# Audit runs explicitly in CI (ci.yml), not on every install
audit=false
